{
  "name": "viral-em-1-minuto",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "vercel-build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
